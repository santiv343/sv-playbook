// Función mínima usada por tests end-to-end de humo (el build/paquete
// realmente se ejecuta, no sólo compila) — a propósito trivial, no es
// código de producto.
export function e2eDummy(): number {
  return 42;
}
