# Idea Backlog

Ideas are never killed — they are recorded here unvalidated. **An entry is
not a commitment** (PRINCIPLE-008): an idea graduates to a plan only when a
real project demonstrates the pain it solves, and the graduation note must
name that evidence. Review this file at every project closure/retro.

Status values: `unvalidated` · `scheduled-v2` (already in the spec's v2
scope) · `graduated` (moved to a plan; keep the row for traceability) ·
`obsolete` (superseded by a later decision; kept as a tombstone, never silently removed).

| ID | Idea | Origin / pain signal | Status |
| --- | --- | --- | --- |
| IDEA-001 | `doctor` — self-diagnosis: node version, DB health, orphan worktrees, stale sessions, valid config | sv-forge had one and it was useful; agents can self-serve when something is off | unvalidated |
| IDEA-002 | `task import` — bulk-convert legacy task documents into canonical packets | Aurora adopt: 81 legacy packets to migrate; hand-retyping is absurd | GRADUATED to packet TASK-IMPORT-001 (2026-07-09) |
| IDEA-003 | Telemetry JSONL export — periodic append-only export of the events table | Honest gap: SQLite is operational truth but event history dies with the DB unless exported; backups snapshot it but are not queryable | unvalidated |
| IDEA-004 | Notifications — push/telegram/whatever when a packet blocks, a PR opens, or a lease goes stale | AFK agentic work: the human is the bottleneck and doesn't know when they're needed | unvalidated |
| IDEA-005 | Packet types (FEA/BUG/DEBT/SEC) with type-specific evidence requirements (e.g. BUG requires reproduction RED test) | Taste graduation table already wants BUG-specific gates | unvalidated |
| IDEA-006 | `retro` — generate a retrospective report from events: blockers, deviations, verify cycles, time per state; semi-automates the learnings loop | Learnings inbox depends on agent diligence; events already hold the facts | unvalidated |
| IDEA-007 | Model registry + routing — config maps packet type/tier to model class; feeds the model×role matrix with observed rework data | Spec §13 matrix is static; events could make it empirical | unvalidated |
| IDEA-008 | Multi-project board — one `serve` over all active projects in a workspace | User runs 3+ projects in parallel with agents in each | unvalidated |
| IDEA-009 | Agent launcher glue — a launch flag on `task next` that spawns the harness preloaded with the brief | Borderline NG2 (we don't orchestrate agents); record, don't judge yet | unvalidated |
| IDEA-010 | Evidence hash-chain — tamper-evident chaining of evidence outputs | Only matters if fabrication survives current gates; no observed pain yet | unvalidated |
| IDEA-011 | `check secrets` — secret scanner as a first-class check target | Real incident: .env with JWT/DB/API keys loose in a non-repo workspace dir for weeks | unvalidated (strong candidate) |
| IDEA-012 | Golden-path example repo — a tiny finished project built 100% through the playbook, as living documentation/portfolio demo | Portfolio value + onboarding; also doubles as an e2e fixture | unvalidated |
| IDEA-013 | GitHub Issues read-only projection — mirror packets outward for mobile/away visibility | Depends on IDEA-004 vs. this; overlapping pains | unvalidated |
| IDEA-014 | Webhooks from CLI events | Superset of IDEA-004; keep separate in case serve wants them | unvalidated |
| IDEA-015 | Sprint/milestone grouping in the board | Jira-like organization once packet count grows | unvalidated |
| IDEA-016 | Watch mode — live-refreshing terminal board without serve | Cheaper interim visibility than serve | unvalidated |
| IDEA-017 | `serve` — local web viewer, live board, metrics dashboards; buttons call the CLI | Spec v2 scope (D13) | scheduled-v2 |
| IDEA-018 | MCP wrapper autogenerated from `describe --json` | Spec v2 scope (D14) | scheduled-v2 |
| IDEA-019 | `agent stop` with a hard mode via declared PID | Spec v2 scope (NG4) | scheduled-v2 |
| IDEA-020 | Taste inference from existing repo code and recent agent-conversation history | Spec §6c backlog note (Command Code parity) | unvalidated |
| IDEA-021 | Custom memory implementation behind the files-first port | Spec §14/D12: explicitly out until files + adapters demonstrably fall short | unvalidated |
| IDEA-022 | `freeze` / `revive` commands automating the tombstone checklist | Spec §5 procedure exists; automation once used manually a few times | unvalidated |
| IDEA-044 | Auto-rotation on provider errors: the (future automated) orchestrator detects 403/limit/4xx worker deaths and rotates to the next harness in the routing table without human involvement | kimi quota death required manual takeover+rotation 2026-07-08; the procedure is charted, the automation is not | unvalidated |
| IDEA-043 | Dispatch adapter catalog (user directive: rotate harnesses): one doc per harness with its non-interactive spawn recipe, prompt passing, kill method and session-log path — opencode (serve+prompt_async/abort, SOLVED), claude -p, codex exec, Command Code (TBD), Kimi (TBD). The worker template stays identical; only the adapter varies. Dispatch events already record (harness, model) → rotating harnesses yields comparative rework/cost telemetry for the routing matrix for free | The founder owns several harnesses and wants to use them all; agent-agnosticism makes that a measurable experiment instead of a risk | GRADUATED same day: content/dispatch/adapters.md — 5/5 harnesses probed headless (kimi 9s, claude 13s, codex 15s, commandcode 46s, opencode via serve API) |
| IDEA-042 | Investigate non-interactive dispatch reliability: `opencode run` hangs at bootstrap (post-config, pre-model) on this install even with --pure/--agent/trivial prompts in small dirs — 4 reproducible hangs 2026-07-08. Ruled out 2026-07-08 (6 reproducible hangs, all frozen post-config-load pre-model, near-zero CPU): default flags, --pure, --agent build, --auto, --format json, trivial prompt in small dir. Remaining suspects: non-TTY stdout spawn on Windows, or globally configured MCP servers booting in run mode (--pure may not skip MCPs). Next probes: user runs `opencode run "hi"` in a real terminal (10s test - if it works there, it is the non-TTY spawn); `opencode serve` + opencode run with the attach flag; `claude -p` as worker harness; upstream issue with this data | The autonomous pipeline (IDEA-027) and board dispatch (IDEA-040) depend on a reliable non-interactive spawn | unvalidated (parked with full repro data) |
| IDEA-045 | Serve information architecture (founder: "necesito saber en todo momento qué está pasando"): (1) operations bar - permanent orchestrator card (state, last action, decision queue) + live worker chips (packet, harness+model, elapsed, last breadcrumb, health semaphore) + review lane; (2) activity feed - one chronological typed-event stream across ALL actors, filterable; ephemeral roles (planner/reviewer/product) are visible as event AUTHORS, never as board rows; (3) kanban stays pure work; (4) escalations panel, visually loud - in full-auto, empty panel means nothing needs the human. All sourced from the existing D23 events table - zero new infra | Cards are work, roles are actors: the board must show both without mixing them | unvalidated (v2 serve blueprint) |
| IDEA-039 | Pipeline autonomy modes (user directive): `pipeline: off | assist | full` in playbook.config.json — off = human dispatches; assist = orchestrator proposes, human confirms per packet; full = ready packets auto-dispatch (IDEA-027). Serve gets a CONFIG PAGE to switch modes (writes via CLI, single write path) | The founder wants to choose per project/moment how much control to keep | unvalidated (config field is trivial; serve page is v2) |
| IDEA-040 | Board dispatch/kill buttons powered by harness non-interactivity: dispatch = `opencode run -f <brief-file>` / `claude -p` spawned by serve backend (PID recorded as dispatch event) — kill = terminate that PID (the hard-stop NG4 gets real because WE spawned the process and own its PID) | First real orchestrator dispatch happened today via opencode run; owning the spawn solves the kill problem that cooperative control could not | unvalidated (v2 with serve) |
| IDEA-041 | Card detail live view — UPGRADED TO REQUIREMENT (founder: "necesito datos reales todo el tiempo, sí o sí"): the card detail must show the SAME data the agent's own CLI shows, live. Mechanism proven 2026-07-08: poll GET /session/{id}/message and render each part (tool + status + input). Orchestrator practice from today: EVERY dispatch arms a transcript tail alongside the board monitor | Board-state monitoring alone left a 20-minute blind window on a running worker | validated mechanism; serve rendering pending (v2) |
| IDEA-035 | Evidence capture kills report verbosity (own idea): once a `verify` command wraps the project verify and records full output + exit code as evidence events (D24 extension), the "paste the COMPLETE verify output" rule becomes obsolete — reports shrink from ~150 lines to ~10, and trust IMPROVES (machine-recorded beats agent-pasted). The paste rule was always a workaround for missing capture | Every report today carries 50 test names x several reads per mission; the reviewer re-derives anyway | unvalidated (next batch candidate) |
| IDEA-036 | Quiet test reporter for worker-local runs (own idea): `npm run test:quiet` with the dot reporter for STEP 6/8/9 loops; workers run tests 4+ times per mission and each run streams ~50 test-name lines into their context. CI/verify keeps the full spec reporter | Thousands of context tokens per mission for zero information the worker uses | unvalidated (trivial) |
| IDEA-037 | Per-packet diff budget (own idea): the planner declares `diff_budget_loc` in the packet; the brief tells the worker the smallest-code target and the reviewer flags overshoot as a finding. Complements ponytail from the SPEC side: the budget is decided at planning, not generation | Smaller diffs = fewer generated tokens, fewer review-read tokens, and a quality signal for free | unvalidated |
| IDEA-038 | Context-size routing (own idea): planner tags packets with estimated context need; the orchestrator/matrix picks the cheapest model whose window suffices, not just by role | Model choice today is per-role; per-packet sizing squeezes the last 2-5x | unvalidated |
| IDEA-033 | Relocate .svp/ OUT of the repo tree (e.g. %LOCALAPPDATA%/sv-playbook/<repo-id>/) | The DB inside the repo sits in the blast zone of every repo-scoped operation: rm -rf habits, git clean -fdx would delete it today; agents think in repo paths — moving coordination state out of their mental map kills the accidental-deletion class | unvalidated (strong candidate) |
| IDEA-034 | `rebuild` refuses when fresh leases exist unless --force (live-process guard) | The EPERM corruption happened because a destructive op ran while other processes held the DB; rebuild should check heartbeats first | **obsolete** — depends on IDEA-029, which was superseded (rebuild command removed 2026-07-08); the live-lease guard now lives in `backup state`/`restore state` --force paths |
| IDEA-031 | Token-economy program: (a) stable-prefix dispatch — static template text first, ASSIGNMENT values at the END so harness prompt caching hits (cache reads cost ~10% of input); (b) same ordering rule inside `task brief` (fixed process sections first, variable packet content last); (c) record best-effort token/cost per session in dispatch events to MEASURE savings; (d) confirm caching enabled per harness | Agent workloads resend full context per tool call (10-100x chat cost); prompt caching alone reported cutting spend ~59% | unvalidated (template fix applied immediately; rest next batch) |
| IDEA-032 | Evaluate the ponytail skill (github.com/DietrichGebert/ponytail) for planner+implementer roles: forces a decision ladder (does this need to exist? stdlib? existing dep?) before writing code; benchmarks claim 54% less code, 22% fewer tokens | Aligns exactly with existing taste ("prefer established libraries", "abstractions are earned") — mechanizes it at generation time instead of review time | unvalidated (trial on one packet, compare events/telemetry) |
| IDEA-029 | `rebuild` command: reconstruct .svp DB from docs/packets/*.md (+ durable stamps) | Real incident: schema gained write_set and the only remedy was a hand ALTER TABLE - the durability rule is a promise without implementation until this exists | **obsolete** — superseded by spec §8/D6 (2026-07-08): SQLite is now operational truth, NOT rebuildable from packet files; durability is via `backup state`/`restore state` instead |
| IDEA-030 | Harness transcript audit: reviewer optionally reads the worker session transcript to catch ephemeral, trace-less deviations | The temp-file trick in LINT-STRICT-001 was invisible in diff and events; only the pasted transcript revealed it | unvalidated (costly; wait for a real incident that justifies it) |
| IDEA-027 | Autonomous dispatch pipeline: a packet entering `ready` auto-dispatches a worker (non-interactive harness CLI: `opencode run` / `claude -p`, model per the model×role matrix); reviewer verdict routes automatically — APPROVED → merge → `done`; REQUEST CHANGES → findings attached as events, packet back to `ready` for ANY available agent; loop until done. Human involvement ends at product/planning (draft), except escalations | The founder's target operating model, stated 2026-07-08 after the first full manual cycle (CLI-CONFIG-001) proved every stage works | unvalidated (the v1.x north star; needs orchestrator charter + D24 evidence capture first) |
| IDEA-028 | Merge→done automation: the merge event closes the packet (reviewer step or git hook), no manual `task move done` | First merged packet needed a manual close | unvalidated (trivial, graduates with IDEA-027) |
| IDEA-025 | Write-set conflict detection at PLANNING time: `task create`/`move ready` warns or fails when the write_set intersects any ready/active packet (today only `start` checks) | Real incident: the planner (Claude) marked LINT-STRICT-001 ready overlapping two in-flight packets — his own charter step 6 missed it manually | unvalidated (strong candidate) |
| IDEA-026 | Missing transitions: `ready -> draft` (misplanned demotion) and `review -> ready` (review rejection releases the packet for any agent, findings attached) | Planner incident + the autonomous-pipeline flow (IDEA-027) | unvalidated (strong candidate) |
| IDEA-024 | `orchestrator` role charter (the active residue of a PM): watches the board, detects stale leases, triggers takeover/escalation, enforces WIP limits, reports to the human | Needed the day 2+ implementers run in parallel (Aurora adoption); today it would be a role with no work | unvalidated (near-certain graduation at parallel execution) |
| IDEA-023 | Duplicate-string lint (e.g. sonarjs no-duplicate-string) in the TS stack preset | PR #3 review: 32 status literals and repeated event/path strings reached production before a human caught them | unvalidated (strong candidate) |
| IDEA-046 | Configurable state machine: project-defined board columns, packet states, and allowed transitions (today hardcoded `draft->ready->active->review->done` + `blocked`/`dropped` in service.constants + schema CHECK constraints) | Founder directive 2026-07-08: some teams need other columns/states/steps; the fixed flow is one opinion, not universal | scheduled-v2 |
| IDEA-047 | `task amend` (or `task create --force`): edit a packet's write_set/title/depends_on/evidence after creation — today the CLI can only change status and append notes post-create; fixing a planning mistake in write_set requires a direct SQLite UPDATE, which violates CLI-single-author | Reviewer PR#23 flagged BACKUP-VERIFY-RESTORE-001's write_set (`src/db/**`) as broader than its real blast radius; narrowing it needed a manual DB update because no amend path exists | GRADUATED to packet TASK-CORE-AMEND-001 (via DB-core redesign; supersedes CLI-TASK-AMEND-001, 2026-07-09) |
| IDEA-048 | Aurora adoption program (founder north star): apply sv-playbook to the real Aurora monorepo — rearrange its repos to this modality and measure how well/badly it fits. First step (read-only, cheap): an agent audits Aurora against the playbook and returns a gap report WITHOUT changing anything. Needs the `adopt` command built (inventory + gap analysis + remediation packets with a baseline) and all v1 gaps covered first. Large: its own planning session with fresh quota | Founder 2026-07-09: "quiero probarlo en Aurora, ver si funciona como debería, que un agente analice qué tan bien o mal se implementa" | unvalidated (big; scheduled for a fresh-quota session; `adopt` is PLANNED) |
| IDEA-049 | Model-relay mechanism (founder: "relevar modelos sin perder contexto ni capacidad"): the durable board (.svp + git) already holds the context; the missing piece is a command that regenerates the orchestrator continuation prompt from live state on demand | Founder wants to swap the active model when quota runs out, cheaply, without a hand-written handoff | GRADUATED to packet HANDOFF-CMD-001 (2026-07-09) |
| IDEA-050 | Configurable roles: project-defined role names, permissions, and role assignments (today hardcoded in the engine — planner, implementer, reviewer, orchestrator) | PRINCIPLE-013 audit: roles are an opinion that varies across teams/projects; some teams need QA, some need security reviewer, some skip the orchestrator | **GRADUATED — verified resolved 2026-07-17**: `src/roles/catalog.constants.ts` + `bundled-profile-bootstrap.ts` — role catalog is DB-versioned with a `bundled` (default) / `custom` (per-project) source, no role-name literals in the engine |
| IDEA-051 | Configurable lint/gate thresholds: project-defined lint strictness levels, gate pass/fail criteria, and severity mappings (today hardcoded thresholds in the engine) | PRINCIPLE-013 audit: what counts as "passing" is a project opinion; strictness varies by tier and team | **GRADUATED — verified resolved 2026-07-17**: `playbook.config.json` → `gates: { maxLines, maxLinesPerFunction, complexity, cognitiveComplexity, layout }`, validated by `GatesConfigSchema` |
| IDEA-052 | Configurable module-layout rule: project-defined file-structure conventions (today the `.types`/`.constants`/`.errors` module-layout rule is hardcoded in the engine) | PRINCIPLE-013 audit: module structure is a project/style opinion; different teams use different conventions | partial (verified 2026-07-17): `gates.layout` is an on/off switch in config, but the `.types`/`.constants`/`.errors` convention itself is still fixed in the engine, not parametrizable — the switch exists, the opinion behind it doesn't |
| IDEA-053 | Configurable kanban columns: project-defined board columns, WIP limits, and column ordering (today hardcoded in the engine and schema) | PRINCIPLE-013 audit: kanban layout is a workflow opinion; teams use different column sets and ordering | scheduled-v2 (confirmed still open 2026-07-17: `ALLOWED` transition map is a hardcoded `Map` literal in `src/tasks/service.constants.ts`, not config-sourced) |
| IDEA-054 | Configurable packet types and ID prefixes: project-defined packet categories and their ID prefix schemes (today hardcoded types and prefixes in the engine) | PRINCIPLE-013 audit: packet classification is a project-domain opinion; different projects need different type taxonomies | partial (verified 2026-07-17): `packets.type` is already free-text with no closed enum blocking it, but there is no formal type registry/validation either — open by accident, not by design |
| IDEA-055 | Configurable tier definitions: project-defined complexity tiers with their gates, budget rules, and evidence requirements (today hardcoded tier definitions in the engine) | PRINCIPLE-013 audit: tier boundaries and their associated rules are opinionated; teams define their own complexity ladder | scheduled-v2 (confirmed still open 2026-07-17: `tierValues = ['TIER-1','TIER-2','TIER-3']` is a fixed enum in `src/schema/config.constants.ts`; what each tier implies is not parametrizable) |
| IDEA-056 | Configurable review checklist: project-defined review rubric items and their severity (today a hardcoded checklist in the engine) | PRINCIPLE-013 audit: what a reviewer checks is a project-quality opinion; teams have different standards and priorities | ambiguous (verified 2026-07-17): lives in `content/review.md`, project-editable prose — already "config" in this repo's own sense that `content/` is the opinion layer, but not *structured* config. Needs a founder call on whether prose-in-content/ counts as resolved or whether this needs to become structured data |
| IDEA-057 | Configurable packet-template required sections: project-defined template structure and mandatory sections (today hardcoded required sections in the engine) | PRINCIPLE-013 audit: template structure is a documentation-style opinion; different orgs want different artifact shapes | scheduled-v2 (confirmed still open 2026-07-17: `REQUIRED_SECTIONS` is an array literal in `src/cli/commands/check.ts`) |
| IDEA-058 | Configurable dispatch routing: project-defined agent/harness routing rules, model-to-role mappings, and spawn recipes (today hardcoded routing logic in the engine) | PRINCIPLE-013 audit: which harness handles which packet type is an infrastructure opinion; teams use different toolchains | partial (verified 2026-07-17): `role_execution_profile_preferences` table exists (DB-driven), but not confirmed whether a hardcoded fallback still exists — needs closer look, not re-verified fully |
| IDEA-059 | CLI inspection surface for the review machinery: `review candidate list/show` (or `task show --json` exposing the current work-definition version and latest candidate id/SHA) | GATE-012 promotion day (2026-07-15): the operator read `.svp` SQLite directly — a PRINCIPLE-012 violation — to find the candidate id, and loop-guessed definition versions for `dispatch prepare`. A missing CLI path is a packet, never a workaround | unvalidated (strong candidate) |
| IDEA-060 | promotion-tables migration must handle aged stores: recreate pre-GATE-012 `promotion_*` DDL (missing `review_candidate_id`/`work_definition_*`; orphan `check_attempts`/`adoptions`/`invalidation_records`) — `CREATE TABLE IF NOT EXISTS` does not update existing tables (ENTRY-008) | First real `promotion run` failed in production with `no such column: review_candidate_id`; the store was repaired by manual drop/recreate outside the machinery. The GATE-012 reviewer flagged the missing aged-state migration fixture as informational; production graduated it within the hour. Needs an ENTRY-011 aged-fixture regression test | unvalidated (strong candidate) |
| IDEA-061 | PromotionController close should honor `backup.onEvents: ["done"]` (invoke `backupForEvent`) or record a decision that the immutable promotion record supersedes the file-level backup | GATE-012 independent review RUN-019f669a (2026-07-15), low finding: previous done paths called `backupForEvent`; `closePromotedTask` does not | unvalidated |
| IDEA-062 | Idempotency guard for clean-verification check receipts on promotion replay (the INTEGRATED→CLOSED recovery window can insert a duplicate receipt for the same candidate) | GATE-012 independent review RUN-019f669a (2026-07-15), informational finding: append-only and safe, but a noisy audit artifact | unvalidated |
| IDEA-063 | UI static-asset encoding gate: no check inspected `content/ui/**` for double-encoded UTF-8, so corrupted text could reach the served console silently (HJ-015: UI is a product surface) | GATE-012 promotion day (2026-07-15): the promotions UI shipped 10 double-encoded sequences — "mecánico", "integración", "Último cambio" rendered as mojibake in the live browser console; only a human eyeballing the page caught it | GRADUATED to packet BUG-019 (2026-07-15) |
| IDEA-064 | `task create` should validate the same REQUIRED_SECTIONS the packets check enforces (`## Task`, `## RED test`, `## Stop conditions`, `## Evidence`), and active packets need an amend path — today intake accepts a body the check later rejects, and once active the only amendment route is release → blocked → ready → amend → start | BUG-019 implementation (2026-07-15): packet created without `## Evidence` passed create, failed check mid-implementation; the blocked-state detour worked but is undiscoverable | unvalidated (strong candidate) |
| IDEA-065 | `serve` shutdown lifecycle: the daemon `/api/v1/shutdown` stops the API but the front HTTP server keeps holding :3131 as an orphan; the operator must `taskkill` the pid before relaunching. Either shutdown should stop the whole process or a separate graceful `serve stop` path should exist | BUG-019/BUG-020 promotion day (2026-07-15): every sqlite inspection detour (IDEA-059) ended with a manual pid kill before serve could restart; happened twice in one session | unvalidated (strong candidate) |
| IDEA-066 | Envelope-kind payload validation beyond review-verdict: other agent outputs (implementation reports, orchestrator decisions) still validate only against the generic `semantic-work-envelope-v1` envelope; when a second kind needs a strict payload, graduate the BUG-020 kind check into a kind→schema registry in `src/contracts` | BUG-020 design note (2026-07-15): one known kind today, so a direct check ships; the registry is earned by the second kind | unvalidated |
| IDEA-067 | Retry path for a terminally failed dispatch: `dispatch prepare` keys identity on (subject, role, phase), so once a run lands `output-invalid`/`failed` the subject is bricked — re-prepare returns the same terminal RUN and `dispatch start` answers `GATEWAY_RUN_ALREADY_TERMINAL`. The only escape today is a fresh task id | Roles/agents stress round (2026-07-15): two sandbox probes (BUG-002, BUG-003) became unreviewable after the agent emitted malformed verdicts; a `dispatch retry --run <id>` minting a fresh attempt on the same subject was sorely missed | GRADUATED — shipped 2026-07-16: `dispatch retry --run` (new module `src/gateway/run-retry.ts` + `run-retry-linkage` migration) mints a chained successor (`retry_of_run_spec_id`, `:retry:N` refs), idempotent via dispatch identity, typed refusals (`RUN_RETRY_NOT_TERMINAL` / `RUN_RETRY_COMPLETED` / `WORKFLOW_RUN_RETRY_IS_ENGINE_OWNED` / `STALE`), profile reloaded from current config; 5/5 tests + live sandbox proof (store migration, idempotency, refusals, recovery resume, attempt-3 chain) |
| IDEA-068 | Windows handle leak on the CLI error path: `dispatch start` over an already-terminal run prints the correct typed error and then aborts with `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING), src\win\async.c line 76`, exit 127 (reproducible 2/2) instead of a clean exit code | Roles/agents stress round (2026-07-15), sandbox probe of BUG-002's terminal run | GRADUATED — shipped 2026-07-16: root cause removed — `dispatchRun` answers terminal runs from the durable snapshot BEFORE any adapter I/O (no fetch → no dangling handle → no crash); typed `GATEWAY_STATE_ERROR` codes; 3/3 sandbox repro clean (exit 1, zero network with the server down) |
| IDEA-069 | `context add` accepts an undeclared `--kind` and the bogus kind then breaks every `context compile` (`no precedence configured for context kind …`); there is no `context delete`, so the repair is supersede + re-precedence by hand. Intake should validate kind against the declared kinds or fail closed | Roles/agents stress round (2026-07-15): one `bogus-kind` item poisoned all role compiles in the sandbox until manually superseded | GRADUATED — shipped 2026-07-16: `context add` validates kind against declared precedence kinds and fails closed with `CONTEXT_ERROR.MISSING_PRECEDENCE` |
| IDEA-070 | `role check` vs runtime gap: the check validated an execution profile missing `adapterConfig.outputMode`, but `dispatch start` rejects that profile with `INVALID_ADAPTER_RESPONSE adapter outputMode must be a string`. Whatever the runtime requires, the check should require first | Roles/agents stress round (2026-07-15): catalog reported `valid:true` with an undispatchable reviewer profile; the failure surfaced only at run time | GRADUATED — shipped 2026-07-16: the check parses `adapterConfig` with the adapter's own parser (`adapterConfig(profile)` in opencode-projection) — undispatchable profile ⇒ invalid at check time |
| IDEA-071 | Phantom migrate-live flag: the migration branch guard's error said "switch to main or pass migrate-live", but no CLI command accepted it (no CLI surface parses "migrate" at all) — the only live-migration path is the library API `openStore(root, { migrateLive: true })` | IDEA-067 sandbox proof (2026-07-16): retrying a terminal run in a sandbox on a feature branch hit the guard; the instructed escape hatch did not exist | GRADUATED — shipped 2026-07-16: guidance text fixed — the guard now points at the audited `migrateLive` library option; exposing a real CLI flag would be a separate feature, not a doc lie |
| IDEA-072 | Provider self-loop starvation: a zhipuai/glm-5.2 OpenCode session delivered a contract-valid verdict in 30s and then kept self-generating "Acknowledged." turns for 40+ min (437 assistant messages, ~10M cached-read tokens, ONE user message total); the gateway observed changing busy-evidence the whole time so (a) `noProgressTimeoutMs` never fired (busy churn counts as progress), (b) completion is only extracted when the session goes idle, so the valid output sat invisible in history, (c) there is no wall-clock/message-count ceiling to force-cancel a runaway provider loop. Needed a manual `/session/:id/abort`. Fixes: progress = new relevant output (message/tool ids), extraction of terminal output independent of idle, and a hard run ceiling | IDEA-067 live retry proof (2026-07-16): RUN-019f6864 observed 285 events over 40 min without ever seeing the verdict that was ready at second 30 | GRADUATED — shipped 2026-07-16: (b) candidate completion — the adapter surfaces the FIRST finished in-flight response as `candidateOutput` and the gateway completes from it when it passes the output contract (provider cancel is best-effort hygiene after the durable commit; a non-conforming candidate just keeps the run observing); (c) hard ceiling `maxRunDurationMs` (optional on profile/run_spec, 30-min engine default, anchored to the durable turn `created_at` so it survives resumes) → `timed-out` + `RUN_DURATION_EXCEEDED`; (a) progress semantics untouched by design. `run-duration-ceiling` migration (execution_profiles + run_specs), CLI `--max-duration-ms`, 25/25 focal tests (RED first) + verify 4/4 + duplicate-strings debt re-baselined 1348→1329; live sandbox proof: BUG-002 retry attempt 3 completed through the terminal path with a valid APPROVED verdict |
| IDEA-073 | `validated-text` outputMode puts contract conformance on the model's honesty (system prompt), which produced lowercase verdicts (BUG-002/003 output-invalid) and leaves OpenCode's own `output_format: json_schema` validation (NATIVE mode, with provider-side retryCount) unused. Evaluate moving reviewer/typed-output profiles to NATIVE so structure is enforced by the provider layer instead of the prompt | Roles/agents stress round + IDEA-072 loop (2026-07-16): two output-invalid runs and one runaway session, all on validated-text profiles | unvalidated |
| IDEA-074 | Retry-chain attempt budget: a configurable cap on total attempts per subject across the retry chain (config, not hardcode — PRINCIPLE-013) and/or a per-dispatch budget rail, so a pathological subject cannot chain retries forever | principles audit 2026-07-16 (PROD-1): IDEA-072's `maxRunDurationMs` bounds each individual attempt but not the chain; a runaway provider loop can still consume N × ceiling | unvalidated |
| IDEA-075 | Daemon-issued session role: the daemon should mint the session-role token (with correct hygiene — unguessable, scoped, revocable) instead of the CLI reading `.svp-session-role`, a file any local process can write; the destructive gate would then authenticate rather than trust a self-declared identity | principles audit 2026-07-16 (PROD-2, medium): the short fix (documenting the trust model in cli.md §Store safety) landed in the audit slice; the mechanized fix remains open | unvalidated |
| IDEA-076 | Remote destination adapter for backups: today backups land on the same disk as the store, which does not survive host loss; a remote adapter (object storage, second host) is a precondition for Aurora/TIER-3 | principles audit 2026-07-16 (PROD-3) | unvalidated |
| IDEA-077 | Event-log retention/archival policy: `gateway_run_events` grows unbounded (~285 events in one live review); add configurable retention by age or run-terminal state, with archival before purge if the audit trail must survive | principles audit 2026-07-16 (PROD-4) | unvalidated |
| IDEA-078 | Automated regression test for the `dispatch start` exit-code contract (0/1/2/3) on terminal runs: IDEA-068 shipped with sandbox proof only; the contract can silently regress | principles audit 2026-07-16 (PROD-5) | unvalidated |
| IDEA-079 | Adapter-secrets audit: verify API keys never enter the context pack, the persisted RunSpec, or the event log — one leak into any of the three durable surfaces is a credential incident | principles audit 2026-07-16 (PROD-6) | GRADUATED — audit executed 2026-07-16: the model holds (secrets enter via env at spawn, never persisted; context pack carries no adapter config). Four hardening findings recorded as IDEA-083..086 |
| IDEA-080 | CLI + store-schema versioning policy (semver, changelog, breaking-change rules) before the second instance (Aurora) exists to drift against | principles audit 2026-07-16 (PROD-7) | unvalidated |
| IDEA-081 | Define the gate vs rail distinction in `content/principles.md` (candidate: gate = a check that blocks, rail = a structure that guides) or consolidate the vocabulary — ~573 vs ~108 uses today with no canonical definition | principles audit 2026-07-16 (NAME-1); decision-ask candidate, needs founder call | GRADUATED — founder decision 2026-07-16: vocabulary section added to `content/principles.md` (gate = mechanical blocker, rail = structural guidance), no renames (PR #139) |
| IDEA-082 | Audit that `verify` actually gates everything the docs claim it gates: the audit sampled the plane but nobody has verified full coverage of the documented gate set | principles audit 2026-07-16 (review follow-up, scope flagged by the independent reviewer) | unvalidated |
| IDEA-083 | Adapter-config secrets persisted in cleartext: `execution_profiles.adapter_config_json` and the per-run `run_specs.execution_profile_json` snapshot persist the adapter config verbatim — the day a profile carries an API key in config (instead of env), the store and every backup become a credential leak. Add a sensitive-field denylist/allowlist at persistence, or a secret-ref indirection | secrets audit 2026-07-16 (P6 finding 1, MEDIA): today keys arrive via env so nothing persists, but nothing MECHANICALLY keeps it that way | unvalidated |
| IDEA-084 | Command-output scrubbing at capture: preflight and promotion check receipts persist up to 4KB of raw `outputTail` per phase; a verify/preparation command that echoes an expanded secret writes it durably into the store and the events log. Scrub captured output against the process env's sensitive values before persisting | secrets audit 2026-07-16 (P6 finding 2, MEDIA) | unvalidated |
| IDEA-085 | Agent output persisted verbatim: `gateway_runs.output_json` stores the model's raw reply; an agent that pastes a credential into its answer writes it to the durable record. Low today (workers have no secret access), real the day Aurora code handles credentials | secrets audit 2026-07-16 (P6 finding 3, BAJA) | unvalidated |
| IDEA-086 | Daemon token visible in the process list: the serve daemon's session token appears in `ps` output, readable by any local process. Move it to an env var or a permissioned file | secrets audit 2026-07-16 (P6 finding 4, BAJA; local-only threat) | unvalidated |
| IDEA-087 | review-candidate-v2 contract deployment gap: stores created before #141 do not carry the `review-candidate-v2` semantic-role contract until someone runs `role bootstrap`; the close path then fails on the old store with a contract error instead of upgrading itself. Make the consumer (review transition / promotion) self-heal by ensuring required contracts at use time, or ship a store migration that inserts missing contract rows | close-path dogfood 2026-07-16 (FLOW-017): live store needed a manual `role bootstrap` before `task move review` would create the candidate | unvalidated (strong candidate) |
| IDEA-088 | Packet-export parity is not mechanically checked: `check structure` only validates sections of files that exist on disk, so it cannot catch (a) a packet row whose export file is missing — ADOPT-INVENTORY-FIX-001 is `done` in the store but its file never reached main (branch chore/plan-adopt-inventory-fix unmerged) — nor (b) semantic drift between the stored work definition and the export on done packets, which `task amend` can no longer regenerate — FLOW-006's file write_set is a strict subset of the stored one. Add a parity check (DB work definition vs parsed export, set-equality on arrays, EOL-normalized body) covering every packet row, including done | DOCS-003 closure evidence 2026-07-16: semantic parity script found exactly these 2 anomalies among 185 packets | unvalidated (strong candidate) |
| IDEA-089 | Daemon build-staleness is silent: store.ts tryAutoForward forwards every CLI invocation to the running daemon and exits with its result, so a daemon booted on an older dist rejects newer commands as "Unknown command" and silently bypasses newer contract/validation code — observed 2026-07-16 when a pre-#146 daemon swallowed enforce (shipped in #146) and every store mutation that window executed under stale code. Add a build-digest handshake: stamp a digest into dist at build time, daemon exposes it, tryAutoForward compares before forwarding and refuses with guidance (or supervised restart) on mismatch | CLI auto-forward debug session 2026-07-16: dist had enforce in the registry while the running CLI rejected it; root-caused to store.js:177 forwardToDaemonSync hitting the stale daemon | unvalidated (strong candidate) |
| IDEA-090 | Promotion TARGET_STALE over-fires on already-integrated candidates: ensureIntegrationAttempt (src/promotion/promotion.integration.ts:148-150) requires refSha(target) === candidate.baseSha even when candidate.candidateSha is already an ancestor of the target ref (integration would be pure bookkeeping). Hit 2x on 2026-07-16 (STORE-005, DOCS-003): a docs-only squash merge advanced local main between candidacy and promotion, both candidates were contained in main with APPROVED verdicts, and the only recovery was full re-candidacy + re-review of an identical tree. Proposal: before the TARGET_STALE throw, if git.isAncestor(candidate.candidateSha, refSha(target)) then record intent and let integrateCandidate reach its existing already-integrated path; keep the strict guard only for real fast-forwards (candidate not yet in target, redteam expectation preserved) | close-path operations 2026-07-16: two APPROVED candidates blocked by PROMOTION_TARGET_STALE after an unrelated docs merge moved local main; recovered via demote → re-candidate → re-review cycle | unvalidated |
| IDEA-091 | `decision ask --packet <id>` parses a `--packet` flag that is never read or persisted (src/cli/commands/decision.ts handleAsk) — the `decisions` table has no packet linkage column at all, so a decision can never be tied to the task that raised it. Either wire it (decisions.packet_id + FK) or drop the dead flag | Architecture audit 2026-07-16 (founder session on decoupling/complexity-checkpoint design): found while evaluating `decision` as the base for a human-approval gate on architecture-significant packets | unvalidated (blocks the complexity-checkpoint design) |
| IDEA-092 | Schema sprawl audit: the live store has grown to 73 distinct tables (grep of CREATE TABLE across src/db) with no periodic review — includes a 7-table `protocol_*` cluster (protocol_proposals, protocol_proposal_batches, protocol_proposal_reviews, protocol_reconciliation_proposals, protocol_reconciliation_reviews, protocol_shared_schemas, protocol_work_packets) undocumented in how-it-works.md/anatomy.md, and possible overlapping work-item concepts (packets / packet_definitions / task_costs / sprints / sprint_tasks) that may encode the same fact in more than one place (PRINCIPLE-011). Needs a dedicated pass: per table, who writes it, who reads it, is it reachable from a documented use case, does it duplicate another table's fact | Founder directive 2026-07-16: "revisar los schemas de la db... están hechos así nomás sin pensar" | unvalidated (strong candidate — feeds PRINCIPLE-015 subtraction pass) |
| IDEA-093 | MCP wrapper for the CLI (already anticipated: `describe --json` emits the command catalog it would consume, per docs/how-it-works.md §13 PLANNED list) — founder hypothesis: agents dispatched today under-use the CLI because they only have shell access to it, not a typed MCP tool surface, which may be a real driver of the "vibe-coded" quality problem this session is addressing | Founder directive 2026-07-16: "creo que los agentes no saben usar bien el CLI porque no tienen el MCP... creo que sale muy fácil" | unvalidated (candidate for the agentic-addon subproject) |
| IDEA-094 | Long-lived loops move into the daemon: today `dispatch start`'s observation loop and similar long-running work run in the invoking CLI process; the proposal moves them to live inside `sv-playbook daemon` (the existing single-blessed-writer process) so they survive the invoking CLI exiting. Design doc proposed phase 1 = observation-only, with a verified line-by-line boundary map | Design session 2026-07-16 (`docs/design/2026-07-16-a1-loops-al-daemon.md`, archived to docs/ARCHIVE.md during the docs cleanup) — item A1 of that day's master plan | unvalidated (design proposed, not implemented — full doc in git history if needed) |
| IDEA-095 | QUICKSTART.md content is stale: it describes a PM/TL/Implementer role chain that `content/taste/human.md` (HJ-020 supersession map) already marked superseded by the human-interface/delivery-orchestrator/refuter model, and its "Where the durable record lives" section still describes packets as git `.md` with the DB as a secondary SoT — the opposite of decision D4 (packets 100% in DB, no git plane) taken 2026-07-16. Needs a rewrite once the packets-in-DB migration (complexity-checkpoint design, Pieza 1) lands, not before | docs/ cleanup session 2026-07-16: kept as one of the 5 core docs but flagged instead of silently treated as accurate | unvalidated (rewrite blocked on Pieza 1 shipping) |
| IDEA-096 | Rename the CLI verb group `task` to `packet`, everywhere, to resolve a 3-way naming collision: today "task" means (1) the CLI command (`task create`, `task move`, ...), (2) an existing packet ID prefix (`TASK-IMPORT-001`, `TASK-CORE-AMEND-001`, `TASK-CORE-DB-001`, `TASK-CORE-SCHEMA-001`, `TASK-AREAS-001`, `TASK-AUTHORING-CLI-001`, `TASK-MD-EXPORT-001`, `TASK-MOVE-NOSTAMP-001`, `TASK-RUBRIC-001` — 9 packets), and (3) the generic prose word for "unit of work". The DB/code already settled on "packet" as the dominant noun (tables `packets`/`packet_definitions`/`packet_deps`, type `PacketDefinition`, dir `docs/packets/`); "task" is the one term out of alignment. Comparison that motivated this: in Jira the generic noun is "Issue" — "Task" is only ONE issue type alongside Bug/Story/Epic, never the generic word. Scope surveyed 2026-07-17: `src/cli/commands/task.ts` (347 lines, all subcommands), 28 internal `src/` modules import from `tasks/` as a path, and 9 files reference `task <verb>` as prose/examples outside src (`content/cli.md`, `content/dispatch/worker.md`, `content/roles/format.md`, `content/roles/implementer.md`, `content/roles/orchestrator.md`, `content/roles/planner.md`, `content/roles/reviewer.md`, `content/skills/repo-state.md`, `docs/QUICKSTART.md`), plus `AGENTS.md`. Needs its own scoped packet — do not fold into the complexity-checkpoint work. New commands designed after 2026-07-17 (`packet history`, `packet diff` from the checkpoint design) already use the `packet` verb so nothing new is built on the name being retired | Founder directive 2026-07-17, during the complexity-checkpoint CLI-command design: "no reinventemos o cambiemos nombre a algo que tiene que ser similar" — flagged mid-design instead of building `task history`/`task diff` under the name about to be retired | unvalidated (scoping done, not yet a packet — decision D9 in `docs/superpowers/specs/2026-07-16-complexity-checkpoint-design.md`) |
| IDEA-097 | **RESOLVED by founder reversal, 2026-07-17 (same session, two messages apart):** config stays CLI-driven after all, matching `PRINCIPLE-013`'s literal text — but "bien validada" (well validated). Needs a `config` CLI command (`config get`/`config set`/`config list`, reusing `PlaybookConfigSchema` Ajv validation) instead of hand-editing `playbook.config.json`. Supersedes the "config files are the interface, no CLI command" direction recorded earlier the same session — kept both messages in this row for the record, since flip-flopping mid-session on an architectural call is itself a pattern worth being able to see later | Founder, 2026-07-17: "lo de config por CLI, me arrepentí, sí quiero que la config de todo sea CLI driven. pero bien validada." | **decided — needs a `config` command as part of closing IDEA-096/checkpoint work; update `docs/superpowers/specs/2026-07-16-complexity-checkpoint-design.md` Sección 4 to match** |
| IDEA-110 | Error-boundary handling feels inconsistent to the founder — checked before logging (D8 discipline): 10 typed error classes exist already, one per module (`ConfigError`, `ContextError`, `DaemonListenError`, `LifecycleError`, `PacketFormatError`, `PromotionError`, `RestoreError`, `SchemaError`, `StoreVersionError`, `WorkDefinitionError`), and exit codes are standardized (`EXIT.OK/GATE_FAIL/USAGE/SYSTEM` = 0/1/2/3). But there are 24 spots across `src/` doing a generic `catch (error)` — not yet verified how many of those correctly map to a typed exit code vs. swallow/rethrow inconsistently. Needs a real audit: for each generic catch, does it (a) re-throw a typed error, (b) map to the right EXIT code, (c) log/report consistently? Candidate for a dedicated "error boundary" pass, possibly its own principle/gate (mechanized check that no catch block silently downgrades a typed error to untyped) | Founder, 2026-07-17: "deberíamos tener algún sistema de boundary de errores o similar. siento que no estamos manejando bien eso" | unvalidated (evidence gathered, full audit not done — scope not to be underestimated, could be sizable) |
| IDEA-098 | Backlog entries go stale silently: re-verifying IDEA-050..058 on 2026-07-17 found 2 already fully resolved (050, 051) and never marked GRADUATED, 3 partially resolved with no status update, and only 3 genuinely still open — all had sat as "scheduled-v2" since a single audit pass with no re-check since. The backlog's own convention (`unvalidated`/`scheduled-v2`/`graduated`/`obsolete`) has no mechanism forcing re-verification against current code before an idea is acted on or cited — anyone (human or agent) reading an old row takes its status on faith. Candidate fix: tie backlog review into the complexity-checkpoint's own D8 (prior-art evidence) — before a packet cites an IDEA as justification, it must show current evidence the IDEA is still accurate, not just link the row | Founder, 2026-07-17: "esto de que quedaron muchas cosas escritas pero no ejecutadas también pasó, hay que ver qué podemos mejorar ahí" — raised directly after finding 2 stale-resolved entries in IDEA-050/051 | unvalidated (meta-process idea, candidate for the same session that resolves IDEA-092's DB schema audit) |
| IDEA-099 | D8 (prior-art evidence, complexity-checkpoint design) needs a concrete mechanism, not just "attach evidence you looked": require an actual grep/regex/codegraph search over keyword(s) derived from the new thing being proposed (table name, symbol name, config key, concept name), with the search command and its output captured as the evidence artifact — not a free-text claim of "I checked". Applies to agents specifically: they must run the search and show the output, the same evidentiary standard as a RED test's captured failure output | Founder, 2026-07-17: "hay que hacer que los agentes revisen cosas que les hagan ruido aunque sea mínimo. que se haga algún grep o búsqueda regex de palabras clave... debemos averiguar bien que no existen cosas que ya estamos por repetir" — direct refinement of D8 in `docs/superpowers/specs/2026-07-16-complexity-checkpoint-design.md`, fold into that spec before it's finalized | unvalidated (blocks final D8 wording) |
| IDEA-100 | **Architecturally foundational, found while refining IDEA-051**: sv-playbook's core must be agnostic not just of agent/harness (already established) but of **work domain** — a packet could be "write a chapter" or "design a logo", not only "write code". Today's `playbook.config.json` → `gates` section (`maxLines`, `complexity`, `cognitiveComplexity`, `layout`) is TypeScript/code-specific opinion sitting in what's supposed to be the opinion-free core — it duplicates what a project's own linter (ESLint or equivalent per language) already does, AND it assumes every project has code at all. Same applies to IDEA-052 (the `.types`/`.constants`/`.errors` module-layout convention — also code/TypeScript-specific). Proposal: these become part of a **"code addon"** (parallel to the agentic addon), not core — core only knows "packet has evidence requirements", not what kind. Founder: "el código es el addon de agentes que pueden tomar las tareas" — code-specific tooling is itself one addon among possible others (writing, design, ops), never assumed. This likely reshapes the 3-piece decomposition (núcleo/addon agéntico/frontend) from earlier in this session into 4: núcleo (domain-agnostic) / addon de código (lint-equivalent gates, language-specific) / addon agéntico (dispatch/adapters) / frontend | Founder, 2026-07-17, refining IDEA-051: "estamos repitiendo el trabajo de eslint no? ... esto es project agnostic así que NO puede tener dependencias a un lenguaje en particular... puede usarse en cualquier proyecto, incluso en proyectos que no son código" | unvalidated (high priority — reframes the whole decoupling effort in `docs/REORG.md`). **Correction 2026-07-17, same day:** traced the actual parse path (`mergeDefaults` in `src/schema/config.constants.ts`) — `gates` is already defaulted via `mergeNested(raw.gates, DEFAULTS.gates)` BEFORE Ajv validation runs, and `eslint.config.js` reads the raw file independently with its own fallback `DEFAULTS`. A non-code project can omit `gates` entirely today with zero friction — the "every project is forced to declare ESLint thresholds" framing was wrong, verified by reading the code, not assumed. The real remaining concern is narrower and lower-urgency: `PlaybookConfigSchema`'s TYPE still bakes in code-specific field names (`maxLines`, `cognitiveComplexity`) as always-present rather than modeling them as optional/addon-owned — a type-purity issue, not a usability blocker. Downgrade from "high priority, needs its own fix" to "low priority, cosmetic, fold into whatever addon-boundary work eventually happens" |
| IDEA-101 | IDEA-050 follow-up: role-catalog customization (bundled/custom source, already verified working) needs a companion validation — if a project customizes/removes roles such that the system can no longer function end-to-end, that must be caught and surfaced (block, or run in an explicitly notified "limited mode"), never fail silently or produce an unusable half-configured state | Founder, 2026-07-17: "que la DB tenga una seed mínima default si el usuario quiere. sino, hay que validar también que el sistema tenga lo mínimo necesario para funcionar completo (o limitado, y notificarlo)" | unvalidated |
| IDEA-102 | IDEA-053 (configurable kanban columns/state machine) confirmed important by the founder directly: "es importante poder configurar el tablero, los estados permitidos... y eso" — no new detail given, keep IDEA-053 as the tracking row, this entry just records the founder's explicit confirmation of priority | Founder, 2026-07-17 | unvalidated (see IDEA-053) |
| IDEA-103 | IDEA-055 (configurable tier definitions) elevated by the founder to a hard requirement, not just "nice to have": "debe ser configurable cada tier sí o sí. lo que tenemos hoy son los que vienen por defecto, pero es indispensable que cada usuario pueda adaptar playbook a lo que necesite y no al revés." — product principle stated directly: the tool adapts to the user's process, never the other way around. TIER-1/2/3 as they exist today become the DEFAULT preset, not the only option | Founder, 2026-07-17 | unvalidated (see IDEA-055; product-principle-level, candidate for content/principles.md alongside PRINCIPLE-013) |
| IDEA-104 | IDEA-054 (packet types/prefixes): founder confirms it needs improvement ("podemos mejorarlo") without further detail yet — needs a follow-up conversation to scope | Founder, 2026-07-17 | unvalidated (see IDEA-054, underspecified) |
| IDEA-105 | IDEA-056 (review checklist) and IDEA-057 (packet-template required sections): founder wants both made "más robusto" (more robust) — no further detail yet, needs a follow-up conversation to scope what "robust" means concretely (structured data instead of prose? per-tier variants? something else) | Founder, 2026-07-17 | unvalidated (see IDEA-056/057, underspecified) |
| IDEA-106 | IDEA-058 (dispatch/agent routing) flagged by the founder for deep, careful analysis before any design: "eso del ruteo de agentes... hay que analizarlo bien bien bien" — explicitly NOT ready to scope yet, do not attempt a quick fix here the way 050/051 got resolved incidentally | Founder, 2026-07-17 | unvalidated (explicitly deferred — needs its own dedicated session) |
| IDEA-107 | Project bootstrapping / "north star" — how does a brand-new project (via `init`, still `PLANNED`, never built per `docs/how-it-works.md` §13) get its vision/principles/sense of direction established, and which role owns that conversation (human-interface? planner? product? some role not yet in the catalog)? Needs real thought, not an incidental fix: what's the interview/wizard flow, what gets written where (constitution? content/? DB?), how does it avoid becoming a rubber-stamp questionnaire vs a genuine judgment-capture process | Founder, 2026-07-17: "cuando se arranca un proyecto de 0, cómo seteamos el norte? principios, sentido... playbook debería contener todo eso y encarrilar todo para ese lado siempre. pero hay que pensarlo" — explicitly wants this thought through carefully, timing undecided ("no sé si ahora o más adelante") | unvalidated (explicitly deferred, must not be lost — founder was explicit about this) |
| IDEA-108 | Existing-project adoption (`adopt`, already partially built — see `ADOPT-AURORA-001`/`ADOPT-BASELINE-001`/`ADOPT-GAP-001`/`ADOPT-INVENTORY-001`/`ADOPT-SCAFFOLD-001` packets, all `done`) — companion question to IDEA-107: when sv-playbook is adopted onto an existing project (Aurora is the named real case, IDEA-048), what can be legitimately INFERRED from the existing codebase/docs vs what must be explicitly asked of the human? Today's `adopt` flow does inventory+gap+scaffold — needs review against this exact question: is it inferring too much (silently assuming intent) or too little (annoying the human with questions it could answer itself) | Founder, 2026-07-17: "playbook puede iniciarse en proyectos ya empezados, ej aurora. y qué pasa ahí... supongo que podemos inferir muchas cosas pero hay que pensarlo" | unvalidated (explicitly deferred, pairs with IDEA-107) |
| IDEA-109 | Per-role/per-agent execution config (which model, which provider, API vs CLI harness, tool permissions) — checked against existing code before logging this (D8 discipline): **already substantially built.** `execution-profile add/set/clone/list` (`src/cli/commands/execution-profile.ts`) already supports `--role`, `--adapter`, `--agent`, `--provider`, `--model`, `--tool <id=allow|deny>` per profile. Not a gap — needs verification/walkthrough against the founder's exact ask before assuming it's fully sufficient, and it belongs under the "addon agéntico" piece of the 4-piece decomposition (`docs/REORG.md`) | Founder, 2026-07-17: "en el addon de agentes... debemos también poder configurar cada rol, cada agente, qué modelo usa, si usa API o CLI, etc." — flagged as wanting deliberate thought, but prior-art check found most of it already exists | unvalidated (verify existing `execution-profile` covers this before building anything new) |
| IDEA-111 | **Principle candidate, not yet added to `content/principles.md` (founder call needed):** the CLI's own command/subcommand/flag surface must be self-discoverable from ONE generated source — `describe`/skills/MCP/human docs all derive from it, never hand-duplicate it. Direct corollary of `PRINCIPLE-011` (single source for every fact) applied specifically to CLI shape. Concrete evidence this is violated today, found while investigating the skills/MCP ecosystem ask: `content/cli.md` literally states in its own header "this guide is the single source for agent-facing CLI usage... do not duplicate its content elsewhere" — but it is itself 100% hand-authored prose, not generated from the command registry, and was caught describing a `docs/constitution/` file-export behavior that `src/cli/commands/constitution.ts` never actually implements (fixed inline 2026-07-17, not a regression from the day's docs cleanup — the doc was already stale before that). `describe --json` (the mechanism that SHOULD be the real single source) today only emits `{name, summary}` per top-level command — no subcommands, no flags, no types — nowhere near enough for MCP tool generation or to replace `content/cli.md`'s hand-written detail | Founder, 2026-07-17: "todas las tools del CLI deben ser autodescubribles... no debemos mantener muchos lugares distintos. principio." | unvalidated — **plan written 2026-07-17:** `docs/superpowers/plans/2026-07-17-self-discoverable-cli.md` (6 tasks: mandatory `Command.usage`, mechanized gate, backfill the 14 commands missing it, wire into `describe`, trim `content/cli.md`'s hand-written syntax). Principle addition to `content/principles.md` still needs a founder call, not resolved by the plan |
| IDEA-112 | Skills/MCP ecosystem for the agentic addon must stay domain-agnostic (ties directly to IDEA-100): skill templates and MCP tool descriptions must not assume the packet being worked is code — "las tareas pueden ser de lo que sean, no solo código. pueden ser diseño, investigación, o cualquier cosa." Only one skill exists today (`content/skills/repo-state.md`, read-only board status) and it's already domain-neutral (calls `status`/`doctor`, no code-specific language) — good precedent to replicate, not a violation to fix | Founder, 2026-07-17: "lo que quiero es que playbook pueda manejarse agénticamente, pero las tareas pueden ser de lo que sean no solo código" | unvalidated (design constraint for whatever skills/MCP plan comes next) |
| IDEA-113 | **Corrected 2026-07-17 (second-pass verification caught a wrong first read):** HJ-020 role-model drift is NOT "the new roles have no charter" — `src/roles/bundled-profile.constants.ts` fully defines all 9 new roles (`human-interface`, `advisor`, `planner`, `refuter`, `arbiter`, `delivery-orchestrator`, `investigator`, `implementer`, `reviewer`) with mission, exclusive judgment, capability request classes, prohibitions, and a full handoff graph — richer than the old prose ever was, and this is exactly the structured schema `role_contracts`/`role_responsibilities`/`role_prohibitions`/`role_stop_conditions`/`role_handoffs` already supports. The REAL gap: `content/roles/*.md` (5 OLD roles: `format`, `implementer`, `orchestrator`, `planner`, `product`) is what `sv-playbook docs roles/<role>` reads — verified `src/content.ts` is 100% filesystem, zero DB awareness. `docs roles/human-interface` returns "Unknown topic" TODAY even though human-interface is fully defined and live in the DB catalog. Fix: retire the 5 stale `.md` files (they describe the superseded model per HJ-020's own supersession map), and make `docs roles/<role>` (and `content.ts` generally) query the DB-resident role catalog instead of/in addition to the filesystem — same "generate from the real source" pattern as IDEA-111 | Founder, 2026-07-17: "eso no vas más en .md, se hace con el seed de la db, los roles por defecto. y tenemos un sot" — first-pass investigation wrongly concluded the new roles needed authoring; corrected after reading `bundled-profile.constants.ts` in full | unvalidated (scope now: retire 5 stale files + make `docs roles/*` DB-aware, not author new charters) |
| IDEA-114 | Cold-start (`AGENTS.md`/`CLAUDE.md`, generated by `sv-playbook instructions --write` from `content/instructions/cold-start.md`) is role-agnostic today — it says "you are an AI agent working under the sv-playbook methodology" but never declares which role the session should assume, and there is no mechanism for a session to auto-bootstrap into a specific role (e.g. human-interface) on open. Founder wants: open a fresh session in this repo, on ANY harness → it automatically knows it's the human-interface role, with the compiled charter, without being told. **Agent-agnostic requirement confirmed against real code (2026-07-17):** `instructions.ts` (`INSTRUCTION_MIRRORS`) only emits 2 mirror files today — `AGENTS.md` and `CLAUDE.md` — not a mirror per harness (no OpenCode-specific, no Codex-specific, no `.cursorrules`). If role-bootstrap content is added, it must land in every mirror the instruction-mirror system emits (and that system may itself need more mirror targets), never just `CLAUDE.md`, or a session opened via OpenCode (this project's primary dispatch harness per `content/dispatch/adapters.md`) would silently not get it — exactly the kind of harness-specific leak `PRINCIPLE-004`/`HJ-005` exist to prevent. Depends on IDEA-113's actual scope (making `docs roles/*` and cold-start generation DB-aware — the role definitions themselves already exist, per IDEA-113's correction) | Founder, 2026-07-17: "cómo hacemos para que yo abra una sesión acá en Claude, en el repo de playbook, y automáticamente ese Claude sepa todo todo todo? que sea mi human interface?" — then, correcting scope: "acordate, agent agnostic, y todo eso" | unvalidated (depends on IDEA-113 shipping first; must not be designed Claude-specific) |
| IDEA-115 | Onboarding (`init` for new projects, `adopt` for existing ones — Aurora is the named real case) must leave sv-playbook 100% functional in one pass, not as a separate manual step — role charters seeded (IDEA-113) and cold-start role-bootstrap (IDEA-114) are part of what "onboarded" means, not optional follow-ups. Directly elevates IDEA-107/108 (previously logged as "explicitly deferred, no urgency") — the founder is now tying them to a hard functional requirement rather than leaving them open-ended | Founder, 2026-07-17: "eso debería ponerse al instalar playbook la primera vez no? es importante eso, el onboarding debe dejar playbook 100% funcional sí o sí, sea proyecto nuevo o empezado" | unvalidated (ties IDEA-107/108/113/114 into one onboarding requirement — needs its own definition pass, not solved inline) |
| IDEA-116 | Semantic/concept search over packets — an agent should be able to find related context/decisions from OTHER packets by concept, not just by exact ID/grep. Checked before logging (D8): zero existing infrastructure — no FTS5, no embeddings, no vector search anywhere in `src/`. Technical trade-off to weigh when this gets designed: SQLite's built-in FTS5 (zero new dependency, local-first, matches `HJ-014`'s "no required cloud service" default, but keyword/lexical only — no true concept matching) vs. embeddings (real semantic matching, but requires a model/API dependency and network calls this local-first tool doesn't have today). Likely candidate: FTS5 first as the local-first default, embeddings as an optional adapter later if FTS5 proves insufficient — same "adopt/adapt/incubate/build" ladder `HJ-005` already prescribes for new capabilities, not designed in depth here | Founder, 2026-07-17: "deberíamos tener algún search semántico de las tareas... cosa de que un agente pueda encontrar fácilmente conceptos y contexto de otras tareas" | unvalidated (candidate for the agentic-addon subproject once packets are DB-resident — Pieza 1 of the checkpoint work) |
